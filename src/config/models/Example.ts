/**
 * Output for adding two numbers.
 */
export type AddTwoNumbers = {
  x: number;
  y: number;
  add: number;
};

/**
 * Output for subtracting two numbers.
 */
export type SubtractTwoNumbers = {
  x: number;
  y: number;
  subtract: number;
};

/**
 * Output for two randomly generated numbers.
 */
export type RandomNumbers = {
  x: number;
  y: number;
};
